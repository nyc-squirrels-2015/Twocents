<h1>All Mantras</h1>
	<div>
		<ul>
			<% @mantras.each do |mantra| %>
			Main Focus:
			<div>
			<a href="/mantra/<%= mantra.id %>">∆<%= mantra.focus %></a>
			<!-- make triangle a separate a link with a class to target with ajax later   -->
			</div>
		<% end %>
		</ul>
	</div>